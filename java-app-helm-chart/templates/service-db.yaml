apiVersion: v1
kind: Service
metadata:
  name: mysql-db
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - port: {{ .Values.mysql.port }}
      targetPort: {{ .Values.mysql.port }}
  selector:
    app: mysql-db
  type: ClusterIP
